# SPEC · 轻打卡 v6

> 唯一真相。代码与本文件冲突时，以本文件为准，冲突处按历史残留处理。
> 最后更新：2026-08-19

## 1. 这东西是什么

一个纯静态网页（PWA），装在手机主屏上，每天记蛋白质、喝水、体重，以及跟着做低冲击力量训练。
无账号、无后端、无云同步，数据只在手机本地。

## 2. 使用者与场景

- 谁用：我自己（大体重减脂期）
- 在哪用：手机浏览器 / 添加到主屏后的 PWA；开发时电脑 Chrome
- 什么时候用：每天记几次蛋白和水；每周三次跟练

## 3. 边界内（v6 负责的事）

**今天页**
- 蛋白质：手记一笔（克数 + 食物名）、三个快捷按钮（一拳蔬菜 / 一份豆 / 果或全谷）、对目标的进度条
- 饮水：+350 / +500 一键累加，对每日目标
- 体重：记录当天体重
- 今天跟练：入口 + 已完成标记；走路单独记（不计入力量次数）
- 本周力量 n/3、近 7 天练了几天

**训练页**
- 动作组选择、强度档位、今天身体状态
- 动作列表 + 开始跟练
- 跟练中：语音播报动作名与倒数、动作示范动图、屏幕常亮、计时器提示音
- 可记录哑铃/器械重量

**数据页**
- 本周力量 n/3、累计训练次数、近 7 天日均蛋白、近 7 天有记录天数、7 日均体重、本周走路分钟
- 近 7 天蛋白柱状、近 4 周体重曲线、动作重量、训练记录列表

**我的页**
- 目标：身高、当前体重、每公斤蛋白系数、每日蛋白目标（可手改或按体重算）、每日饮水目标
- 偏好：提示音、语音指导、语音音色（含试听）、动作动画开关
- 数据：导出备份 JSON、导入备份、清空本机数据

## 4. 明确不做

这一节的作用是让下一个模型不要"顺手帮你补上"。

- **不做拍照识别蛋白质。** 2026-08-19 已整体移除（连同 BYOK API Key 机制、两套 relay 实现、阿里云 FC 与 Vercel 部署路径，净删 1200+ 行）。
  理由：它一个功能拖着一整套后端基础设施，是这个项目复杂度失控的唯一来源。想记就手记一笔。
  **注意：历史数据里 `source:'photo'` 的条目仍须正常显示，删的是入口不是数据兼容。**
- **不做任何需要后端的功能。** 没有账号、没有服务端、没有 API Key。`serve.py` 只是本地静态文件服务器。
- 不做云同步 / 多设备同步。换设备走导出/导入备份。
- 不做社交、排行、分享。
- 不做饮食数据库 / 食物库检索。
- 不引入前端框架（React/Vue/…）。`app.js` 就是一个 IIFE。

## 5. 技术形态

- 形态：纯静态网页 + Service Worker（PWA），无构建期转译，`app.js` 是直接跑的 ES5 风格 JS
- 部署：**Netlify 为唯一生产**（`npm run build` → `dist/`）。GitHub Pages 是静态镜像。
- 运行时依赖：**零**。无 CDN、无外部请求（CSP `default-src 'none'`，`connect-src 'self'`）
- 开发期依赖：`playwright`（仅测试）、`python3`（仅本地静态服务）
- 数据在哪：`localStorage`（主）+ `IndexedDB`（备份镜像），全在浏览器本机

## 6. 验收：怎么算这版是好的

```bash
npm run build && npm run check:dist      # 产物校验：46 个 m4a 在位，源码不外泄
python3 serve.py 8951 &
TEST_BASE_URL=http://127.0.0.1:8951 npm run test:e2e   # 127/127 通过
```

外加手机上肉眼确认：

1. 断网能打开（SW 离线缓存生效）
2. 跟练时语音正常播报（46 个 m4a 都能放）
3. 记一笔蛋白 → 今天页进度条更新 → 数据页柱状图出现
4. 导出备份 → 清空数据 → 导入备份 → 数据完整回来
